# Kit archives — models are committed extracted; see README for sources
assets/*.zip

# Intermediate pipeline artifacts
output/raw.glb
output/character.fbx

# Internal working notes, kept local
docs/*-design.md

.DS_Store
.claude/

# Downloaded scenes whose author/licence I haven't confirmed yet — not
# published until attribution is in place. backrooms_vr.glb is committed
# because its licence (CC-BY, carlcapu9) is known.
assets/scenes/dreamcore_liminal_space.glb
assets/scenes/liminal_pool_room.glb
assets/scenes/vr_liminal_room_baked.glb
inspect.html
