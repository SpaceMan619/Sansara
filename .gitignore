# Kit archives — models are committed extracted; see README for sources
assets/*.zip

# Intermediate pipeline artifacts
output/raw.glb
output/character.fbx

# Internal working notes, kept local
docs/*-design.md
docs/superpowers/

.DS_Store
.claude/

# CC-BY scenes are published with attribution. abhayexe's VR Liminal Room is
# under Sketchfab's Standard licence, which allows use in a project but not
# redistribution of the model file, so it stays local.
assets/scenes/vr_liminal_room_baked.glb
output/pool_baked.glb
inspect.html

# Local aircraft pipeline outputs are regenerated from the credited source.
# Dark Snow's Vite build mirrors public files into dist, so ignore that copy too.
experiments/dark-snow/dist/vehicles/aircraft/

# Local scratch experiment: unbuilt, unlinked, and carries the uncompressed
# source aircraft. Not part of the published site.
experiments/pale-horizon/rwanda-flight/
